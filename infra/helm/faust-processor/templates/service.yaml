apiVersion: v1
kind: Service
metadata:
  name: {{ include "faust-processor.fullname" . }}
  labels:
    {{- include "faust-processor.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "faust-processor.selectorLabels" . | nindent 4 }}
  ports:
    - name: web
      port: {{ .Values.service.port }}
      targetPort: web
